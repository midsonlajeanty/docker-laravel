#!/bin/sh

cd /var/www/html

if grep -qiE '^[[:space:]]*SCHEDULER_ENABLED[[:space:]]*=[[:space:]]*false' .env 2>/dev/null; then
    echo "   INFO  Scheduler is disabled, sleeping."
    exec sleep infinity
fi

echo "   INFO  Scheduler is enabled, starting..."
exec php artisan schedule:work
