#!/bin/sh

cd /var/www/html

if grep -qiE '^[[:space:]]*HORIZON_ENABLED[[:space:]]*=[[:space:]]*false' .env 2>/dev/null; then
    echo "   INFO  Horizon is disabled, sleeping."
    exec sleep infinity
fi

echo "   INFO  Horizon is enabled, starting..."
exec php artisan horizon
